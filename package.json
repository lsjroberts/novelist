{
  "scripts": {
    "start": "npm run compile && npm run app",
    "start:dev": "elm-live ./src/Main.elm --output ./app/elm.js --open",
    "app": "electron ./app/electron",
    "compile": "elm make ./src/Main.elm --output ./app/elm.js || true",
    "build": "sudo rm -rf ./dist && npm run compile && electron-packager ./app Novelist --all --version=1.4.11 --out=./dist --prune=false",
    "build:osx": "sudo rm -rf ./dist && npm run compile && electron-packager ./app Novelist --platform=darwin --arch=x64 --version=1.4.11 --out=./dist --prune=false"
  },
  "dependencies": {
    "electron": "^1.4.12",
    "electron-debug": "^1.1.0",
    "electron-packager": "^8.4.0",
    "octicons": "^5.0.1"
  }
}
